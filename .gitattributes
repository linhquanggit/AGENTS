# Keep shell scripts LF so Git-for-Windows autocrlf never marks them modified
# (which would abort updates in a managed checkout). PowerShell scripts stay CRLF.
* text=auto
*.sh           text eol=lf
bin/ai-runtime text eol=lf
*.ps1          text eol=crlf
