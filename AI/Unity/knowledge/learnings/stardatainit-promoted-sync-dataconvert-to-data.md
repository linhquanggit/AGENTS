# Sync StarDataInit PROMOTED: DataConvert → Data phải giữ StarLevelUpgradeFactor

Scope: Assets/Project/Resources/{DataConvert,Data}/*.asset dùng chung class `StarDataInit` (Fighters, Wingmans, và các entity tương tự) | Evidence: Fighters.cs:1443 (class StarDataInit), Assets/Project/Resources/Data/Fighters.asset, Assets/Project/Resources/DataConvert/Fighters.asset, Assets/Project/Resources/Data/Wingmans.asset, Assets/Project/Resources/DataConvert/Wingmans.asset

`DataConvert/*.asset` là output của CSVToFighter/CSVToWingman từ file CSV (Assets/Project/SetupData/FileCSV/<Entity>/Upgrade-PROMOTED-Star.csv) và luôn set `StarLevelUpgradeFactor = 0`; `Data/*.asset` là data thật đang chạy game (NORMAL giữ `0.03`, PROMOTED giữ `0.09`). Khi copy khối `Upgrades` Rank:1 (PROMOTED) từ DataConvert sang Data, chỉ copy HP_Base/DMG_Base/CD_Base/MedalCount/PiecesType/Stars — **giữ nguyên** `StarLevelUpgradeFactor` ở đích, không copy đè. Verify bằng cách decode `Golds` (chuỗi hex, mỗi 8 ký tự = 1 uint32 little-endian, 10 giá trị/star) rồi so với CSV Upgrade-PROMOTED-Star.csv (cột `<ENTITY>_N`, hàng `Gold{star}_{1..10}`/`PieceCount{star}`/`Factor{star}`). Số lượng entry ở Data thường nhiều hơn DataConvert (entity mới thêm chưa có trong CSV) — chỉ sync phần trùng ID, giữ nguyên phần dư ở Data.
