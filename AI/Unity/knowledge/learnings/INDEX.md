# Learnings Index

Project-specific facts discovered during work — patterns, code locations, undocumented conventions, corrected behaviors. Read this index first; open only the entries relevant to the current task. Each entry is one tiny file under `learnings/`.

Entry line format: `- [title](slug.md) — hook (scope)`

<!-- entries below, one line each, newest first -->
- [Snapshot: TestingTools — loại bỏ Odin, thay bằng Unity thuần (CHỜ THỰC THI)](../snapshots/odin-testingtools-migration-plan.md) — plan đầy đủ 15 file (1 root + 14 Tabs), migration map attribute Odin→native, thứ tự tăng dần theo rủi ro, sidebar dọc trái đã chốt, shim PropertyTree tạm thời khi migrate dở; scope CHỈ module `Assets/QL_Tools/TestingTools/`, Odin vẫn giữ cho phần còn lại project cho tới khi gỡ toàn bộ sau này (TestingTools / Odin migration)
- [Sync StarDataInit PROMOTED: DataConvert → Data phải giữ StarLevelUpgradeFactor](stardatainit-promoted-sync-dataconvert-to-data.md) — DataConvert (từ CSV) luôn zero StarLevelUpgradeFactor, chỉ copy HP_Base/DMG_Base/CD_Base/MedalCount/PiecesType/Stars sang Data, giữ nguyên field này; verify Golds hex little-endian với CSV Upgrade-PROMOTED-Star.csv (Fighters, Wingmans)
- [Snapshot: WorldBoss cache 4 file enum + vị trí dùng (baseline hiện tại, cache cho lần pull main TIẾP THEO)](../snapshots/worldboss-premerge-enum-cache.md) — đã restore xong lần 1 (PopupId→204-211, IAP_Product→157-161, REWARD_TYPE giữ 60000-60006, EnumVariables không đổi); bổ sung vị trí MỚI hay bị bỏ sót: bảng giá `idProductIAP` (CC_IAP, key theo TÊN) trong scene `Menu.unity` — thiếu 5 dòng aa_worldboss1-5 gây "mất hết gói WorldBoss" dù không liên quan renumber; nhớ audit vị trí serialize PHẢI quét cả `*.unity` chứ không chỉ `*.asset`/`*.prefab`
- [Snapshot: Catalog module dùng RewardByProgression (PVP BattlePass, TODO Milestone)](../snapshots/reward-by-progression-catalog.md) — living doc so sánh cách các module cắm RewardByProgression; `BP_` = "By Progress" (KHÔNG phải BattlePass) — bảng giá `RewardByProgressionConfig` là nguồn chân lý DÙNG CHUNG toàn game theo thiết kế (~20 alias có sẵn), không phải cấu hình riêng theo module; case 1 = PVP BattlePass (RewardMultiplier, GOLD → multiplier cho GoldByChapter) (Assets/Project/RewardByProgression + BattlePass + Events/MilestoneProgression)
- [Abyss timeout timer đua với die/boss-die (die ưu tiên)](abyss-timeout-race-die-boss.md) — State không đổi đồng bộ với null-hoá FighterCurrent/remove lstEnemies -- 4 fix: freeze theo FighterCurrent==null, AllEnemiesCleared, pendingWinCheck resolve ở OnPlay(), IsLevelRepairBlocked (LevelAbyssManager.cs/LevelManagers.cs/GamePlayController.cs)
- [BossLevelData: DamageDeal tuyệt đối → BaseDamage(level) × Multiplier(phase)](worldboss-bosslevel-basedamage-multiplier.md) — GetPhaseDamageDeal helper thay field DamageDeal đã xoá; GetPreviousProgressData đổi thành GetPreviousMilestoneDamage (trả BigDouble); bẫy milestone#1=0 phải fallback BaseDamage sang phase 2 ở level 1 (WorldBossSO.cs)
- [Bullet mới: UbhBulletStopAndFall (2 phase move → freeze → free-fall)](ubh-bullet-stopandfall.md) — move bình thường → freeze → rơi tự do thẳng xuống, rotation set 1 lần lúc chuyển fall (không set mỗi frame) (Assets/Project/Bullets/Scripts)
- [Bullet UbhBullet: không gắn Rigidbody2D Dynamic/Gravity](ubh-bullet-no-rigidbody2d-dynamic.md) — Rigidbody2D Dynamic+GravityScale trên bullet prefab đấu với script tự set position → quỹ đạo cong ngoài ý muốn; chỉ cần Collider2D trigger (bullet prefabs)
- [Bullet subclass tự return sớm trong UpdateMove() làm kẹt pool](ubh-bullet-custom-updatemove-skip-onupdatedestroy.md) — phase custom-movement không gọi base.UpdateMove() bỏ lỡ OnUpdateDestroy() → bullet trôi khỏi camera không bao giờ release, m_shooting kẹt true, Shot() lần sau no-op (UbhBullet subclass)
- [Snapshot: Catalog Shoot/Bullet Pattern (UniBulletHell + BaseShotE)](../snapshots/shoot-templates/00-overview.md) — 2 hệ song song: UniBulletHell `UbhBaseShot` (28 pattern hình học, không damage/cooldown) vs project `BaseShotE` (~29 class, có damage/cooldown/wave, dùng thật cho enemy/boss); cầu nối `UbhShotE`; trước khi làm đạn/shoot mới PHẢI quét catalog này (01–05) xem tái sử dụng được không (Assets/UniBulletHell + Assets/Project/Enemies/.../BaseShot + Weapons/)
- [Snapshot: Kiến trúc EnhancedScroller + recipe scroller mới](../snapshots/enhancedscroller-architecture.md) — Awake() chỉ tự dựng Container lúc runtime (không Edit Mode); đã có sẵn base generic `LeaderboardScrollerBase<T,TCell>`/`LeaderboardItemBase<T>` nhưng nhiều controller viết tay không dùng; case đại diện: LeaderboardPVP (trần trụi), LeaderBoardWorldBoss (dùng base đúng), ScrollerWorldBossPass (multi-cell + gesture + jump) (EnhancedScroller / toàn project)
- [Snapshot: WorldBoss Addressable audit + plan (CHỜ THỰC THI)](../snapshots/worldboss-addressable-plan.md) — WB thiếu CẢ 5 mắt xích addressable (306 file/51MB chỉ là tên folder); plan P0–P5 ~48MB, còn treo quyết định Remote/Local; đã chốt KHÔNG làm 8 cell prefab (EnhancedScroller đồng bộ) và KHÔNG làm UI-B; bẫy `m_SourcePrefab` = nested instance chứ không phải field spawn (WorldBoss / build size)
- [Phương pháp verify Addressables + bẫy double-wire](../addressables.md) — "tích Addressable" KHÔNG loại asset khỏi build; Resources/ ship vô điều kiện; asset vừa tích vừa wire trực tiếp = ship 2 lần và Analyze KHÔNG bắt được case này; kèm detector `AddressableMapSprite` (đo được 140 prefab / ~12MB trong project) (toàn project)
- [`event_index` = id biến thể/boss (rotate & lặp), KHÔNG phải season id](event-index-not-season-id.md) — **2026-07-29: backend rotate trong danh sách boss ĐÃ RELEASE, không còn cứng 1→6; client không map, chỉ guard `WorldBossConfig.Released`**; detect mùa mới vẫn phải dùng `start_time`, không dùng event_index (Events + WorldBoss)
- [Parse date server: các module event-connect dùng DateTimeStyles.AdjustToUniversal](date-parse-adjusttouniversal-convention.md) — MIXED: event-connect (BattleTeam/AirDefense/Milestone + WorldBoss đã align) dùng AdjustToUniversal; RoundtripKind vẫn còn ở UIEvent/ItemFrame/Sweep — không quét-đổi toàn project (Assets/Project Events + WorldBoss)
- [Snapshot: World Boss UI pre-merge (PopupBase load/show/hide)](../snapshots/worldboss-ui-premerge.md) — chụp 7 popup kế thừa PopupBase (PopupId 200–207) + contract PopupBase + điểm vào, để CONVERT sang hệ UI mới sau khi merge branch main (refactor Popups); companion MonoBehaviour ngoài scope liệt kê riêng (WorldBoss UI / migration prep)
- [JsonUtility: base DTO chiếm field → parse qua class-view](jsonutility-parse-view-pattern.md) — không đa hình, không remap key, không shadow cùng tên; cần list typed khi kế thừa base → parse qua view riêng (mẫu UIWorldBossLeaderBoard.LeaderboardView); WB ConnectResponse : EventConnectResponse + total_damage_all, không fallback score (DTO/JSON toàn project)
- [WorldBoss: 2 biến damage = 2 hợp đồng server, không merge](worldboss-damage-two-vars.md) — UserTotal (từ ngày 1) = leaderboard score; UserReal (sau fake, tập con) = server SUM → total_damage_all; damage = string BigDouble.ToString (Assets/Project/WorldBoss)
- [Hệ popup leaderboard root ở EventLeaderboardEntry](leaderboard-popup-event-entry-root.md) — entry module mới kế thừa EventLeaderboardEntry; 1 response class/module (connect ≡ leaderboard shape); server luôn chèn entry isCurrentUser; EventConnectResponse.cs cấm sửa (Scripts/Leaderboard* + Events)
- [Bẫy: LeaderboardPopupBase.OnSuccess sort theo score int, ghi đè rank](leaderboard-popup-score-sort-trap.md) — module rank bằng giá trị ngoài score (WB: BigDouble) phải override OnSuccess khi nối GetLeaderboardEvent (Scripts/LeaderboardPopupBase.cs)
- [Pattern moveset lao: bay hẳn ra ngoài màn rồi về thẳng](charge-return-moveset-pattern.md) — lấy đà lùi (OutQuad, không FaceVelocity) → lao Linear tới pOut ngoài CameraBouns → về THẲNG (quay đầu khuất ngoài màn, khỏi lo cong/warp); tắt collider ngoài rìa để né ColliderDestroyEnemies; FaceVelocity dùng RotateTowards; geometry gom BuildDashPath + OnDrawGizmos (Enemies moveset)
- [QLMonitor - hub config cho mọi editor tool](ql-monitor-editor-config-hub.md) — tool expose `public static bool Enabled` (EditorPrefs), thêm 1 dòng DrawToggleRow; QLToggle iOS-style (Assets/QL_Tools/Editor)
- [Menu.SetChecked → CS0117](unity-menu-setchecked-cs0117.md) — API không resolve ở Unity 2021.3.45f2, đừng dùng tick menu; vẽ toggle trong EditorWindow thay thế (Editor scripts)
- [Editor crash SIGBUS recompile/Enter Play = UDP socket com.unity.ide.visualstudio sống sót qua domain reload](vs-integration-udp-socket-domain-reload-crash.md) — fix bằng Assets/Editor/VsMessagerReloadGuard.cs (beforeAssemblyReload→Shutdown); LOẠI Hot Reload/Best HTTP/native plugin (Editor crash / VS Code integration)
- [Di chuyển module prefs-riêng vào Player.Data](prefs-module-to-player-data.md) — cached process + EnsureProcess + migrate-then-DeleteKey, LuckyWheel làm mẫu (Assets/Project save system)
- [int.ShortToString() - helper rút gọn K/M/B/T reward count](reward-count-short-format.md) — ưu tiên ShortToString() thay vì FormatString()/ToReadableFormat() (DP.Utilities)
- [WorldCup gold reward = hệ số nhân (GoldByChapter)](worldcup-gold-by-chapter.md) — Count trong WorldCup.asset là multiplier, quy đổi qua WorldCup.EventGold (Events/WorldCup)
- [Networking Core (ApiResponse/HttpClient) không được sửa](networking-core-no-edit.md) — bóc JSON lỗi qua Error string thay vì sửa Core (Networking/Core)
